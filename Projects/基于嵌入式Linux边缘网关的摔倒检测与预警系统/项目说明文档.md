
> 项目名称：摔倒检测边缘网关（Fall Detection Gateway）

> 技术栈：C++17 / Rockchip RKNN / OpenCV / MQTT / SQLite / FastAPI

> 运行平台：Rockchip NPU（RK3566）aarch64 开发板（OrangePi 等）

---
## 目录

1. [项目概述](项目说明文档.md#1-项目概述)

2. [项目背景与意义](项目说明文档.md#2-项目背景与意义)

3. [系统总体架构](项目说明文档.md#3-系统总体架构)

4. [技术选型](项目说明文档.md#4-技术选型)

5. [核心模块详细设计](项目说明文档.md#5-核心模块详细设计)

6. [摔倒判定算法详解](项目说明文档.md#6-摔倒判定算法详解)

7. [数据流与线程模型](项目说明文档.md#7-数据流与线程模型)

8. [性能指标与量化数据](项目说明文档.md#8-性能指标与量化数据)

9. [关键技术难点与解决方案](项目说明文档.md#9-关键技术难点与解决方案)

10. [目录结构](项目说明文档.md#10-目录结构)

11. [编译、部署与运行](项目说明文档.md#11-编译部署与运行)

12. [配置系统说明](项目说明文档.md#12-配置系统说明)

13. [云端接收后端](项目说明文档.md#13-云端接收后端)

14. [项目亮点总结](项目说明文档.md#14-项目亮点总结)

15. [未来展望](项目说明文档.md#15-未来展望)

  

---
## 1. 项目概述

本项目是一个**基于边缘计算的摔倒检测系统**，运行在 Rockchip NPU（RK3566）开发板上。系统通过 USB 摄像头实时采集画面，利用 **YOLOv8n-Pose 人体骨骼关键点模型**在 NPU 上做硬件加速推理，提取人体的 17 个骨骼关键点，再由**规则引擎**依据「身体轴线角度 + 髋部下坠速度 + 时序状态机」综合判断是否发生摔倒。

一旦确认摔倒，系统会：

1. **本地报警** —— 通过 Sysfs 操控 GPIO 引脚驱动蜂鸣器；

2. **现场取证** —— 从环形缓存中异步合成摔倒前 N 秒的短视频（MP4）；

3. **云端上报** —— 通过 MQTT 将报警事件 JSON 推送到云端；

4. **断网容灾** —— 网络中断时报警数据自动落盘 SQLite，网络恢复后补传（Store-and-Forward）。

系统采用**多线程 + 线程安全队列**解耦的设计，将「采集 → 推理 → 判定 → 报警」串成一条低延迟、可容错的流水线，最终落地为一个可直接部署在边缘设备的可执行程序。

---
## 2. 项目背景与意义

### 2.1 社会背景

人口老龄化加剧，独居老人、养老院、医院病房等场景中的跌倒（摔倒）是导致老年人意外伤害甚至死亡的主要原因之一。跌倒后若不能及时发现和救助，会造成严重后果。传统的穿戴式设备（手环、报警按钮）存在老人不愿佩戴、忘记充电、跌倒后无法主动触发等问题。
### 2.2 技术背景

- **摄像头视觉方案**无需老人配合，无感监测；

- **边缘计算**：将 AI 推理下沉到设备端，数据无需上传云端即可完成分析，具备低延迟、保护隐私、弱网可用等优势；

- **NPU 加速**：Rockchip RK3566 内置 NPU（0.8 TOPS），可在低功耗下运行神经网络推理，适合长期无人值守的嵌入式场景。
### 2.3 项目意义

- 实现 **7×24 小时无感、自动、实时的摔倒检测**；

- 边缘端完成核心判断，**数据不出本地**，保护隐私；

- 报警链路完整：**本地声光报警 + 现场视频取证 + 云端通知家属**；

- 具备**弱网/断网容灾能力**，保障关键报警不丢失。

---
## 3. 系统总体架构

系统由 **7 个功能模块**在入口 `apps/main.cpp` 中融合组装，通过线程安全队列解耦，形成「生产者—消费者」流水线。

```

                          ┌──────────────────────────────────────────────┐
                          │                边缘网关（开发板）               │
                          │                                              │
  ┌──────────────┐        │   ┌──────────────┐     ┌──────────────────┐   │
  │ USB 摄像头/    │───────▶│ CameraStreamer │────▶│  frameQueue      │   │
  │ 本地视频文件   │  抓帧   │  (采集线程)     │     │ (线程安全队列)     │   │
  └──────────────┘        │   └──────┬───────┘     └────────┬─────────┘   │
                          │          │ 同步缓存帧              │             │
                          │          ▼                       ▼             │
                          │   ┌──────────────┐     ┌──────────────────┐   │
                          │   │ VideoCacher  │     │ RKNNInferencer   │   │
                          │   │ (环形缓存)    │     │ (NPU 硬件推理)    │   │
                          │   └──────┬───────┘     └────────┬─────────┘   │
                          │          │                      │             │
                          │          │  摔倒时异步合成短视频  │ 17关键点+NMS │
                          │          │                      ▼             │
                          │          │              ┌──────────────────┐   │
                          │          │              │ FallRuleEngine   │   │
                          │          │              │ (角度+速度+状态机) │   │
                          │          │              └────────┬─────────┘   │
                          │          │                       │ 确认摔倒     │
                          │          │                       ▼             │
                          │          │              ┌──────────────────┐   │
                          │          └─────────────▶│   alertQueue     │   │
                          │                         │ (线程安全队列)     │   │
                          │                         └────────┬─────────┘   │
                          │                                  │             │
                          │        ┌───────────┬─────────────┼──────────┐  │
                          │        ▼           ▼             ▼          │  │
                          │ ┌────────────┐ ┌─────────┐ ┌─────────────┐  │  │
                          │ │BuzzerCtrl  │ │MqttClient│ │LocalDatabase│  │  │
                          │ │(蜂鸣器报警) │ │(云上报)  │ │(断网容灾)    │  │  │
                          │ └────────────┘ └────┬────┘ └─────────────┘  │  │
                          └─────────────────────┼───────────────────────┘  │
                                                │ MQTT (QoS 1)              │
                                                ▼                           │
                                     ┌──────────────────────┐              │
                                     │ 云端 MQTT Broker      │              │
                                     │ (broker.emqx.io)      │              │
                                     └──────────┬───────────┘              │
                                                ▼                           │
                                     ┌──────────────────────┐              │
                                     │ FastAPI 云端后端       │              │
                                     │ (接收/存储/推送家属)   │              │
                                     └──────────────────────┘              │
```

  

### 3.1 模块职责一览

| 模块                 | 所属命名空间        | 职责                            |
| ------------------ | ------------- | ----------------------------- |
| `ThreadSafeQueue`  | `concurrency` | 线程安全队列（生产者—消费者模型），解耦各流水线环节    |
| `CameraStreamer`   | `vision`      | 摄像头 / 本地视频采集，独立线程抓帧           |
| `VideoCacher`      | `vision`      | 环形缓存滑窗，摔倒时异步合成前 N 秒短视频        |
| `RKNNInferencer`   | `vision`      | NPU 硬件推理，解码 17 个骨骼关键点 + NMS   |
| `FallRuleEngine`   | `vision`      | 身体轴线角度 + 髋部下坠速度 + 时序状态机判定摔倒   |
| `MqttClient`       | `network`     | 与云端 MQTT Broker 长连接，发布报警 JSON |
| `LocalDatabase`    | `utils`       | SQLite 本地容灾，断网积压、恢复补传         |
| `BuzzerController` | `hardware`    | 通过 Sysfs 操控 GPIO 触发蜂鸣器        |
| `ConfigManager`    | `utils`       | 单例配置管理器，加载并分发全局参数             |
| `SysLogger`        | `utils`       | 单例异步日志系统（spdlog）              |

  ---
## 4. 技术选型

| 技术                          | 用途        | 选型理由                                              |
| --------------------------- | --------- | ------------------------------------------------- |
| **C++17**                   | 主语言       | 高性能、适合嵌入式；`std::string_view`、`std::variant` 等现代特性 |
| **Rockchip RKNN Runtime**   | NPU 推理    | RK3566 官方推理运行时，直接调用 NPU 硬件加速                      |
| **YOLOv8n-Pose**            | 人体骨骼检测    | 轻量级姿态估计模型，输出 COCO 17 关键点，适合边缘端                    |
| **OpenCV 4**                | 图像处理/视频编码 | 抓帧、letterbox 预处理、VideoWriter 视频合成                 |
| **Paho MQTT C++**           | 云通信       | 异步客户端，支持自动重连，适合物联网弱网场景                            |
| **SQLite3**                 | 本地容灾      | 嵌入式文件数据库，无需独立服务，断网落盘                              |
| **spdlog + fmt**            | 日志        | 高性能异步日志，滚动文件，`flush_on(error)` 防死机丢日志             |
| **nlohmann/json**           | JSON 序列化  | 头文件式 JSON 库，解析配置、组装报警报文                           |
| **CMake + aarch64 交叉编译**    | 构建        | 一键交叉编译到目标板                                        |
| **FastAPI + paho-mqtt**（云端） | 云端后端      | 接收报警、提供 REST 查询接口                                 |

---
## 5. 核心模块详细设计

### 5.1 ThreadSafeQueue —— 线程安全队列

**文件**：`include/fall-detection/concurrency/ThreadSafeQueue.hpp`

采用 C++ 模板实现，可复用为图像队列（`cv::Mat`）和报警队列（`AlertEvent`）。核心设计：

- **互斥锁 `std::mutex` + 条件变量 `std::condition_variable`** 保证并发安全；

- **`push`**：队满时自动丢弃最旧元素（防止采集线程跑太快撑爆内存），并使用 `std::move` 转移所有权，避免大图像拷贝；

- **`wait_and_pop`**：阻塞式获取，队列空时线程休眠（**零 CPU 轮询**），直到 `notify_one` 唤醒；

- **`wait_for_and_pop`**：带超时的阻塞获取，用于主循环和报警线程周期性检查退出标志，实现优雅退出；

- 使用 `std::lock_guard` / `std::unique_lock`（RAII 智能锁），异常时自动解锁，杜绝死锁。

### 5.2 CameraStreamer —— 摄像头采集

**文件**：`src/vision/CameraStreamer.cpp` + `.hpp`

- **双模式**：构造函数重载支持 **USB 摄像头**（`/dev/video0`，强制 `CAP_V4L2`）与 **本地视频文件**（用于回归测试）；

- **独立采集线程**：在 `captureLoop()` 中抓帧，避免后端 AI 推理/网络波动阻塞采集；

- **MJPG 格式锁定**：`capture_.set(CAP_PROP_FOURCC, 'MJPG')`，解决部分 V4L2 驱动的抓图死锁问题；

- **视频循环播放**：视频文件播到末尾自动重置帧指针，实现循环回归测试；

- **限帧**：通过动态 `sleep` 将采集速率限制在约 30 FPS；

- 抓到的每一帧同时 `push` 到帧队列和视频缓存器。

### 5.3 VideoCacher —— 短视频异步缓存

**文件**：`src/vision/VideoCacher.cpp` + `.hpp`

- **环形缓冲（Ring Buffer）**：用 `std::deque` 维护固定长度滑动窗口（默认 90 帧，约 3 秒），新帧入队、旧帧出队；

- **异步合成**：报警触发时，先在锁内快速**拷贝快照**（最小化锁占用），再启动独立线程用 `cv::VideoWriter` 编码为 MP4，**不阻塞主线程**；

- **编码器容错**：优先 `H.264(avc1)`，若精简编译的 OpenCV 无 libx264 则**回退 `MPEG-4(mp4v)`**；

- **偶数宽高修正**：H.264/MPEG-4 要求宽高为偶数，奇数会被向下取整，避免写出无法播放的损坏文件；

- **生命周期保障**：后台编码线程句柄存于成员 `writerThreads_`，析构时统一 `join`，确保退出前视频写完（避免缺 `moov` 头无法播放）。

### 5.4 RKNNInferencer —— NPU 硬件推理（系统「大脑」）

**文件**：`src/vision/RKNNInferencer.cpp` + `.hpp`

负责加载 `.rknn` 模型、执行 NPU 推理，并完成 17 骨骼点解码与 NMS。

**初始化流程 `init()`**：

1. 读取 `.rknn` 模型文件到内存；

2. `rknn_init()` 初始化 NPU 上下文；

3. `rknn_query()` 查询输入/输出张量属性（数量、维度、元素个数）。

**推理流程 `detect()`**：

1. **预处理**：BGR→RGB，再 **letterbox 等比缩放**（保持宽高比，灰边 114 填充），避免画面拉伸变形；

2. **输入**：将图像数据指针直接交给 NPU（`RKNN_TENSOR_UINT8` / `NHWC`）；

3. **推理**：`rknn_inputs_set` → `rknn_run` 一键硬件计算；

4. **输出解析**：`want_float=1` 强制 NPU 将 INT8 结果反量化为 float32；

5. **后处理（解码 + NMS）**：

   - YOLOv8-Pose 输出布局为每个锚框 56 个属性：

     `[0..3] 边框 cx,cy,w,h`、`[4] 类别置信度(person)`、`[5..55] 17 个关键点(x,y,conf)`；

   - 过滤置信度低于阈值的锚框；

   - 边框与关键点坐标**逆映射回原图**（先减 letterbox 灰边偏移 `padW/padH`，再除以缩放比例 `scale`）；

   - **NMS（非极大值抑制）**：按置信度降序排序，逐框计算 IoU，剔除重叠的重影框。

6. **内存释放**：`rknn_outputs_release` 释放 NPU 输出内存，防止泄漏。

### 5.5 FallRuleEngine —— 摔倒规则引擎

**文件**：`src/vision/FallRuleEngine.cpp` + `.hpp`

将「静态关键点」转化为「动态摔倒判定」，详见 [第 6 节](项目说明文档.md#6-摔倒判定算法详解)。

### 5.6 MqttClient —— 云通信

**文件**：`src/network/MqttClient.cpp` + `.hpp`

- 使用 **Paho MQTT C++ 异步客户端**（`mqtt::async_client`）；

- **连接配置**：`clean_session(true)`、心跳保活 `keep_alive_interval`（默认 20s，适应弱网）、`set_automatic_reconnect(1, 10)` **断网自动重连**；

- **发布**：将 `AlertEvent` 用 nlohmann/json 组装为标准 JSON，以 **QoS 1（至少到达一次）** 发布：

  ```json

  {

    "device_id": "OrangePi_Gateway_001",

    "event_type": "FALL_DETECTED",

    "timestamp": 1720000000000,

    "data": { "trigger_x": 320, "trigger_y": 240, "status": "CRITICAL" }

  }

  ```

### 5.7 LocalDatabase —— 本地容灾（Store-and-Forward）

**文件**：`src/utils/LocalDatabase.cpp` + `.hpp`

- 基于 SQLite3，`alerts` 表结构：`id`（自增主键）、`timestamp`、`trigger_x`、`trigger_y`、`status`（`pending`/`uploaded`）；

- **`saveAlert`**：断网时把报警落盘，`status` 记为 `pending`；

- **`getPendingAlerts`**：查询所有 `pending` 记录，供网络恢复后补传；

- **`markAsUploaded`**：补传成功后把对应记录标记为 `uploaded`；

- **业务逻辑（在 main.cpp 报警线程中）**：网络在线时，先遍历补传所有历史 `pending` 事件，再发布当前最新报警；发布意外失败也落盘保存，实现**报警事件不丢失**。

### 5.8 BuzzerController —— 蜂鸣器

**文件**：`src/hardware/BuzzerController.cpp` + `.hpp`

- 通过 Linux **Sysfs** 操控 GPIO：`export` 导出引脚 → 设置 `direction=out` → 写 `value` 控制高低电平；

- **异步报警**：`triggerAlarm` 启动独立 `detach` 线程，拉高电平 → 睡眠 `durationMs` → 拉低电平，**不阻塞报警处理主流程**；

- 析构时释放引脚（写 0 并 `unexport`）。

### 5.9 ConfigManager —— 全局配置（单例）

**文件**：`src/utils/ConfigManager.cpp` + `.hpp`

- **单例模式**（禁用拷贝），启动时加载 `configs/config.json`；

- **所有 getter 内置默认值**：配置文件缺失、解析失败或字段缺失时，系统按默认值继续运行，**不会启动失败**；

- 提供**类型安全的命名 getter**（如 `getBuzzerGpioPin()`）与**通用点号取值接口**（如 `getInt("vision.frame_queue_size", 3)`）；

- `find()` 按 `.` 拆分 key 逐级下钻 JSON 节点，未找到返回 `nullptr`。

### 5.10 SysLogger —— 异步日志（单例）

**文件**：`src/utils/SysLogger.cpp` + `.hpp`

- 基于 **spdlog 异步日志器**，独立后台线程写磁盘（线程池队列 8192、1 线程），**写日志不阻塞 AI 推理主线程**；

- **双输出**：彩色控制台 + 滚动文件（单文件 5MB、最多保留 3 个，防止 SD 卡被日志占满）；

- **`flush_on(error)` 容灾**：遇到 ERROR 级别日志立即强制刷盘，防止程序段错误死机时关键现场日志丢失；

- 提供 `LOG_TRACE` ~ `LOG_CRITICAL` 宏，带源码文件与行号定位。

---
## 6. 摔倒判定算法详解

摔倒判定是系统的核心逻辑，位于 `FallRuleEngine::processFrame()`，采用 **「几何特征 + 运动特征 + 时序状态机」** 三层融合策略，有效区分「摔倒」与「坐下/弯腰/蹲下」等易混淆动作。
### 6.1 COCO 17 关键点

采用 YOLOv8-Pose 标准 COCO 关键点索引，本算法只用其中 4 个：

| 索引  | 名称            | 用途    |
| --- | ------------- | ----- |
| 5   | 左肩 L_SHOULDER | 计算肩中点 |
| 6   | 右肩 R_SHOULDER | 计算肩中点 |
| 11  | 左髋 L_HIP      | 计算髋中点 |
| 12  | 右髋 R_HIP      | 计算髋中点 |

### 6.2 特征一：身体轴线角度（几何特征）

取肩中点 `(shoulderX, shoulderY)` 与髋中点 `(hipX, hipY)`（两侧都可见取平均，否则取可见侧），计算身体轴线与**垂直方向**的夹角：

```

dx = hipX - shoulderX

dy = hipY - shoulderY

angle = atan2(|dx|, |dy|) * 180 / π

```

- 站立时身体接近垂直，`angle` 很小；

- 躺倒时身体接近水平，`angle` 接近 90°；

- 当 `angle > fall_angle_threshold`（默认 60°）时，判定为「疑似躺倒」。

> 该特征能过滤「弯腰捡东西」（躯干倾斜但未到躺倒程度）。

### 6.3 特征二：髋部下坠速度（运动特征）

利用相邻两帧的髋部 Y 坐标变化率刻画下坠速度：

```

velocityY = (hipY - previousHipY) / dt

```

- 图像坐标系 **y 轴向下**，摔倒时髋部快速下坠，`velocityY` 为正且数值大；

- 当 `velocityY > fall_velocity_threshold`（默认 400 px/s）时，标记「快速下坠事件」进入时序窗口。

> 该特征能区分「摔倒」（快速下坠）与「缓慢坐下/躺下」（速度慢）。

### 6.4 特征三：时序状态机（防抖 + 双路径）

引擎维护多个状态变量：

| 状态变量                                                 | 含义              |
| ---------------------------------------------------- | --------------- |
| `lieConfirmCount_`                                   | 躺倒连续帧计数（防抖）     |
| `fallEventPending_`                                  | 快速下坠事件是否仍在时序窗口内 |
| `fallEventFrames_`                                   | 下坠事件发生后经过的帧数    |
| `hasPreviousTarget_` / `previousHipY_` / `lastTime_` | 上一帧目标状态（算速度用）   |

**报警判定（双路径 OR）**：

- **路径 A —— 动态摔倒**：`快速下坠事件仍在窗口内` 且 `连续躺倒 ≥ confirm_frames_threshold`（默认 5 帧）；

- **路径 B —— 静态摔倒（兜底）**：`连续躺倒 ≥ static_lie_threshold`（默认 30 帧，约 1 秒）。

> 路径 A 捕捉「明显下坠」的典型摔倒；路径 B 兜底「可能没抓到下坠瞬间」的缓慢摔倒或画面中已躺倒的情况，二者结合提高召回率。

### 6.5 状态重置机制

- 当前帧**未检测到人**、**关键点不足 17 个**、**肩/髋不可见**（置信度低于阈值）时，均调用 `resetState()` 清空状态；

- 目标**姿态恢复**（`angle` 回到阈值内）时，`lieConfirmCount_` 归零并解除报警；

- 触发报警后立即 `resetState()`，**避免同一事件重复报警**。

### 6.6 判定流程图

```
        ┌──────────────┐
        │  输入一帧结果  │
        └──────┬───────┘
               ▼
        ┌──────────────┐   否
        │ 检测到人?     │────────▶ resetState → 返回 false
        └──────┬───────┘
               │ 是
               ▼
        ┌──────────────┐   否
        │ 肩/髋关键点可见?│────────▶ resetState → 返回 false
        └──────┬───────┘
               │ 是
               ▼
    ┌──────────┴───────────┐
    │ 计算角度 angle + 速度 v │
    └──────────┬───────────┘
               ▼
    ┌──────────────────────┐
    │ angle > 60° ?         │
    │ v > 400 px/s ?        │
    └──────────┬───────────┘
               ▼
    ┌──────────────────────────────┐
    │ 时序状态机（防抖 + 双路径）      │
    │  路径A: 快速下坠 + 连续躺倒 ≥ 5  │
    │  路径B: 持续躺倒 ≥ 30          │
    └──────────┬───────────────────┘
               │ 满足任一
               ▼
    ┌──────────────────────┐
    │  确认摔倒 → 生成 Alert │
    └──────────────────────┘

```


---
## 7. 数据流与线程模型

### 7.1 数据流
```

CameraStreamer ──▶ frameQueue ──▶ RKNNInferencer ──▶ FallRuleEngine ──▶ alertQueue ──▶ MqttClient / LocalDatabase

```
### 7.2 线程模型（3 个主线程 + 若干辅助线程）

| 线程         | 位置                              | 职责                              |
| ---------- | ------------------------------- | ------------------------------- |
| **采集线程**   | `CameraStreamer::captureLoop()` | 持续抓帧，投递到帧队列与视频缓存                |
| **主流水线线程** | `main()` 主循环                    | 从帧队列取帧 → NPU 推理 → 规则判定 → 投递报警队列 |
| **报警响应线程** | `main()` 中 `alertThread`        | 从报警队列取事件 → 蜂鸣器 + 补传 + MQTT 发布   |
| **视频编码线程** | `VideoCacher` 后台                | 异步合成 MP4 短视频                    |
| **蜂鸣器线程**  | `BuzzerController` 内部 `detach`  | 异步蜂鸣，不阻塞                        |
| **日志线程**   | spdlog 线程池                      | 异步写日志文件                         |

### 7.3 优雅退出机制

- 全局标志 `volatile std::sig_atomic_t g_running`，`SIGINT/SIGTERM` 信号处理器**只做标志位翻转**（不加锁、不分配内存，保证信号安全）；

- 各线程循环通过带超时的 `wait_for_and_pop` 周期性检查 `g_running`，安全退出；

- 主线程退出前 `stop()` 采集线程、`join()` 报警线程、`disconnect()` MQTT，释放所有资源。

---
## 8. 性能指标与量化数据

> 说明：本节将数据分为两类。**「确定性参数」** 来自代码与 `config.json`，可直接在答辩中引用；**「运行时指标」**（NPU 延迟、端到端帧率、准确率等）需要上板实测，本仓库当前未提交实测结果（`tests/`、`benchmarks/`、`tools/`、`logs/` 均为空目录），故以「参考量级 + 测量方法」形式给出，请上板运行后填入真实值，避免答辩时被追问无法佐证。
### 8.1 确定性参数（源自代码 / 配置）

#### 8.1.1 模型与硬件规格

| 项目     | 数值                                 | 说明                    |
| ------ | ---------------------------------- | --------------------- |
| 检测模型   | YOLOv8n-Pose                       | 姿态估计，输出 COCO 17 关键点   |
| 模型参数量  | 约 3.2M（公开规格，参考）                    | 轻量级，适合边缘端             |
| 模型输入尺寸 | 640×640（letterbox 到模型要求尺寸）         | 由 `inputAttrs_` 运行时读取 |
| 模型精度   | FP16（`do_quantization=False`）      | 未做 INT8 量化，避免精度损失     |
| 目标平台   | RK3566（`target_platform='rk3566'`） | 内置 NPU 0.8 TOPS       |
| 类别数    | 1（仅 `person`）                      | 每个锚框 56 属性            |

#### 8.1.2 推理阈值

| 参数                     | 默认值  | 作用              |
| ---------------------- | ---- | --------------- |
| `confidence_threshold` | 0.5  | 目标置信度过滤         |
| `nms_threshold`        | 0.45 | NMS 交并比剔除       |
| `kpt_conf_threshold`   | 0.3  | 关键点置信度（低于视为不可见） |

#### 8.1.3 摔倒判定阈值（时间换算基于 30 FPS）

| 参数                         | 默认值      | 时间/物理含义               |
| -------------------------- | -------- | --------------------- |
| `fall_angle_threshold`     | 60°      | 身体轴线与垂直方向夹角           |
| `fall_velocity_threshold`  | 400 px/s | 髋部下坠速度                |
| `confirm_frames_threshold` | 5 帧      | 动态摔倒连续确认 ≈ **0.17 s** |
| `static_lie_threshold`     | 30 帧     | 静态兜底 ≈ **1.0 s**      |
| `fall_event_window`        | 15 帧     | 下坠事件有效窗口 ≈ **0.5 s**  |

#### 8.1.4 采集与缓存

| 参数                   | 默认值      | 含义                            |
| -------------------- | -------- | ----------------------------- |
| 采集限帧                 | 约 30 FPS | `captureLoop` 中 `sleep(30ms)` |
| `video_cache_frames` | 90 帧     | 环形缓存，≈ **3.0 s** 预录时长         |
| `video_save_fps`     | 30       | 短视频落盘帧率                       |
| `frame_queue_size`   | 3        | 帧队列容量（队满丢最旧）                  |
| `alert_queue_size`   | 10       | 报警队列容量                        |

#### 8.1.5 网络与报警

| 参数                   | 默认值                              | 含义            |
| -------------------- | -------------------------------- | ------------- |
| MQTT QoS             | 1                                | 至少到达一次        |
| `keep_alive_seconds` | 20 s                             | 心跳保活间隔        |
| 自动重连                 | `set_automatic_reconnect(1, 10)` | 1s 起、10s 封顶重连 |
| `alarm_duration_ms`  | 3000 ms                          | 蜂鸣时长 3 s      |
| `buzzer_gpio_pin`    | 138                              | GPIO 引脚号      |

#### 8.1.6 日志与容灾

| 参数         | 数值                             | 含义                |
| ---------- | ------------------------------ | ----------------- |
| 日志滚动       | 单文件 5 MB × 3 个                 | 最多 15 MB，防 SD 卡写满 |
| 异步日志队列     | 8192 条 / 1 线程                  | 不阻塞推理主线程          |
| SQLite 容灾表 | `alerts`（`pending`/`uploaded`） | 断网落盘、恢复补传         |

### 8.2 运行时指标（待实测，需上板后填入）

以下指标需在目标板实测。建议的测量方法见 8.3。

| 指标          | 参考量级（RK3566 常见，待实测） | 测量方法                |
| ----------- | ------------------- | ------------------- |
| NPU 单帧推理延迟  | 数十毫秒量级              | `rknn_run` 前后打点求均值  |
| 端到端帧率（FPS）  | 受推理延迟主导             | 统计主循环 1 秒处理帧数       |
| 摔倒检测准确率/召回率 | 待标注测试集评估            | 见 8.3 测试方案          |
| 报警端到端时延     | 摔倒发生 → 云端收到         | 时间戳差值               |
| 内存占用        | 待测                  | `top` / `free` 长期观察 |
| 短视频合成耗时     | 待测                  | 编码线程起止时间            |

### 8.3 建议的测量与测试方案（上板前准备）

1. **推理延迟**：在 `RKNNInferencer::detect()` 的 `rknn_run` 前后用 `std::chrono` 打点，累计 1000 帧求均值；

2. **端到端帧率**：主循环每次成功 `detect` 计数，每秒打印 FPS；

3. **准确率评估**：准备标注好的测试视频集（含站立、行走、坐下、弯腰、躺下、摔倒等动作），统计：

   - **准确率 Accuracy** = 正确判定数 / 总样本数；

   - **召回率 Recall** = 正确报警的摔倒数 / 真实摔倒总数；

   - **误报率** = 误报警数 / 总样本数（重点评估「坐下/弯腰」是否被误判）；

4. **断网容灾验证**：运行中拔网线 → 触发摔倒 → 确认落盘 SQLite → 恢复网络 → 确认自动补传；

5. **长期稳定性**：连续运行 24h+，观察内存是否增长、日志滚动是否正常、有无线程泄漏。

> 答辩建议：以上 8.1 的确定性数据可放心引用；8.2 的运行时指标务必先实测并填入真实值，再上答辩。

---
## 9. 关键技术难点与解决方案

### 难点 1：采集与推理速率不匹配，内存易被挤爆

**方案**：引入**有界线程安全队列**（`ThreadSafeQueue`），队满时丢弃最旧帧；配合 `std::move` 转移图像内存所有权，避免大图深拷贝，控制内存占用与采集节奏。
### 难点 2：V4L2 摄像头抓图死锁 / 画面拉伸

**方案**：强制指定 `CAP_V4L2` 驱动并锁定 MJPG 格式规避死锁；模型侧通过 **letterbox 等比缩放 + 灰边填充**保持宽高比，避免画面拉伸变形，同时保证模型输入尺寸一致。
### 难点 3：NPU 输出解析复杂（锚框解码 + NMS）

**方案**：根据 YOLOv8-Pose 输出布局（每个锚框 56 属性），实现**边框/关键点坐标逆映射**（减 letterbox 偏移、除缩放比例）与 **NMS + IoU** 去重，将 NPU 原始张量解析为结构化 `DetectResult`。
### 难点 4：摔倒与「坐下/弯腰/蹲下」易混淆

**方案**：采用 **「角度 + 速度 + 时序状态机」多特征融合** ，几何特征过滤弯腰，运动特征过滤缓慢坐下，时序防抖过滤瞬时抖动，双路径（动态+静态）兼顾召回率与鲁棒性。

### 难点 5：弱网/断网导致报警丢失

**方案**：**Store-and-Forward 容灾**——报警先经 `isConnected()` 探针判断，在线则补传历史积压再发当前；离线或发送失败则落盘 SQLite，网络恢复后自动补传并标记 `uploaded`，保证**报警事件不丢失**。
### 难点 6：短视频合成阻塞主线程 / 写出损坏文件

**方案**：**环形缓存 + 快照拷贝 + 后台异步编码**，锁内只做快速快照；编码器 H.264 回退 MPEG-4 容错；宽高向下取偶避免写坏文件；析构统一 `join` 后台线程，避免缺 `moov` 头无法播放。
### 难点 7：长期无人值守的设备稳定性

**方案**：信号驱动的**优雅退出**；spdlog **异步日志**不阻塞主流程；滚动日志防 SD 卡写满；`flush_on(error)` 保证死机前关键日志落盘；析构释放 NPU 上下文、摄像头、数据库、MQTT 连接。
### 难点 8：跨平台交叉编译依赖管理

**方案**：`aarch64-toolchain.cmake` 指定交叉编译器与 sysroot；`fmt`/`spdlog` 通过 `FetchContent` 源码拉取编译，规避 sysroot 缺 `.so` 问题；`--allow-shlib-undefined` 允许运行期由设备系统库解析符号。

---
## 10. 目录结构

```
fall-detection-gateway/
├── apps/                          # 入口
│   ├── main.cpp                   # 网关主程序（组装各模块）
│   └── main.py                    # 云端接收后端（FastAPI + MQTT）
├── include/fall-detection/        # 头文件
│   ├── concurrency/
│   │   └── ThreadSafeQueue.hpp    # 线程安全队列
│   ├── hardware/
│   │   └── BuzzerController.hpp   # 蜂鸣器
│   ├── network/
│   │   └── MqttClient.hpp         # MQTT 客户端
│   ├── utils/
│   │   ├── ConfigManager.hpp      # 配置管理器
│   │   ├── LocalDatabase.hpp      # SQLite 容灾
│   │   └── SysLogger.hpp          # 异步日志
│   └── vision/
│       ├── CameraStreamer.hpp     # 摄像头采集
│       ├── FallRuleEngine.hpp     # 摔倒规则引擎
│       ├── RKNNInferencer.hpp     # NPU 推理
│       └── VideoCacher.hpp        # 视频缓存
├── src/                           # 各模块实现（与 include 对应）
├── configs/
│   └── config.json                # 运行时配置
├── models/                        # 模型文件
│   ├── yolov8n-pose.onnx / .rknn  # 姿态模型（ONNX 源 + RKNN 转换产物）
│   ├── best.onnx / .rknn          # 备选模型
│   ├── check*.onnx                # 转换调试中间产物
│   └── convert_rknn.py            # RKNN 转换脚本
├── sysroot/                       # aarch64 交叉编译依赖 sysroot
├── lib/                           # 第三方库（rknn-toolkit2）
├── bin/
│   └── fall_detection_gateway     # 交叉编译产物
├── aarch64-toolchain.cmake        # 交叉编译工具链配置
└── CMakeLists.txt                 # 构建脚本

```

---
## 11. 编译、部署与运行
### 11.1 交叉编译

需先安装 `aarch64-linux-gnu-gcc/g++`：

```bash

mkdir -p build && cd build

cmake .. -DCMAKE_TOOLCHAIN_FILE=../aarch64-toolchain.cmake

make -j$(nproc)

```

产物输出到 `bin/fall_detection_gateway`。

**依赖来源**：`fmt`、`spdlog` 通过 `FetchContent` 源码拉取编译；`rknnrt`、`paho-mqtt`、`sqlite3`、`OpenCV` 来自 `sysroot/`。

### 11.2 模型转换

```bash

cd models

python convert_rknn.py   # yolov8n-pose.onnx → yolov8n-pose.rknn

```

转换配置：目标平台 `rk3566`，`do_quantization=False`（保留 FP16，避免 INT8 量化精度损失）。
### 11.3 运行

```bash

# 使用摄像头（设备号由 config.json 配置）

./fall_detection_gateway

# 回放本地视频（用于回归测试）

./fall_detection_gateway /path/to/test.mp4

```

> 运行前确认 `configs/config.json` 与模型文件 `./yolov8n-pose.rknn` 就位。

---
## 12. 配置系统说明

系统启动时由单例 `ConfigManager` 加载 `configs/config.json` 并向各模块分发参数。**所有字段均有内置默认值**，配置缺失或字段不全时按默认值运行，不会启动失败。

| 分组            | 关键字段                                     | 默认值                         | 说明           |
| ------------- | ---------------------------------------- | --------------------------- | ------------ |
| `system`      | `log_file_path` / `log_level`            | `logs/gateway.log` / `INFO` | 日志路径与最低级别    |
| `vision`      | `camera_device_id`                       | `0`                         | 摄像头设备号       |
|               | `frame_queue_size`                       | `3`                         | 帧队列容量        |
|               | `video_cache_frames`                     | `90`                        | 滑动窗口历史帧数     |
|               | `video_save_fps`                         | `30`                        | 短视频落盘帧率      |
|               | `video_output_dir`                       | `videos`                    | 短视频输出目录      |
| `model`       | `rknn_model_path`                        | `./yolov8n-pose.rknn`       | 模型路径         |
|               | `confidence_threshold` / `nms_threshold` | `0.5` / `0.45`              | 置信度 / NMS 阈值 |
| `rule_engine` | `kpt_conf_threshold`                     | `0.3`                       | 关键点置信度阈值     |
|               | `fall_angle_threshold`                   | `60.0`                      | 身体倾角阈值（度）    |
|               | `fall_velocity_threshold`                | `400.0`                     | 下坠速度阈值（px/s） |
|               | `confirm_frames_threshold`               | `5`                         | 动态摔倒连续确认帧数   |
|               | `static_lie_threshold`                   | `30`                        | 静态兜底躺倒帧数     |
|               | `fall_event_window`                      | `15`                        | 下坠事件有效窗口（帧）  |
| `network`     | `mqtt_broker`                            | `tcp://broker.emqx.io:1883` | MQTT Broker  |
|               | `mqtt_client_id`                         | `OrangePi_Gateway_001`      | 设备唯一标识       |
|               | `alert_topic`                            | `fall_detection/alerts`     | 报警主题         |
|               | `keep_alive_seconds`                     | `20`                        | 心跳保活间隔       |
| `hardware`    | `buzzer_gpio_pin`                        | `138`                       | 蜂鸣器 GPIO 引脚  |
|               | `alarm_duration_ms`                      | `3000`                      | 蜂鸣时长         |
| `storage`     | `sqlite_db_path`                         | `fall_detection.db`         | 容灾数据库路径      |
 

---
## 13. 云端接收后端

`apps/main.py` 是一个**演示性的云端接收服务**（FastAPI + paho-mqtt），用于验证端到端链路：

- **订阅** MQTT 报警主题 `fall_detection_gateway/alerts`；

- **接收**网关报警 JSON，用 Pydantic 模型校验结构；

- **存储**到内存列表（实际项目应替换为 MySQL / InfluxDB）；

- **触发**通知服务（实际应对接微信小程序推送 / 短信 API）；

- **提供 REST 接口**：

  - `GET /` —— 健康检查；

  - `GET /api/alerts` —— 拉取历史报警记录；

  - `GET /docs` —— Swagger 自动接口文档。
  - 
启动：
```bash

pip install fastapi "uvicorn[standard]" paho-mqtt

cd apps

uvicorn main:app --host 0.0.0.0 --port 8000

```
---
## 14. 项目亮点总结

1. **软硬件协同的边缘 AI 方案**：YOLOv8n-Pose + Rockchip NPU 硬件加速，低功耗、低延迟、本地推理保护隐私；

2. **多线程流水线架构**：线程安全队列解耦「采集—推理—判定—报警」，采集不卡顿、推理不阻塞；

3. **多特征融合的摔倒判定**：角度 + 速度 + 时序状态机，双路径（动态/静态）兼顾准确率与召回率，抗混淆、防抖；

4. **完整的报警闭环**：本地蜂鸣器 + 现场短视频取证 + MQTT 云端上报；

5. **Store-and-Forward 容灾**：SQLite 断网落盘、恢复补传，报警不丢失；

6. **工程健壮性**：优雅退出、异步日志、滚动文件、资源析构释放、编码器回退、宽高取偶等细节打磨；

7. **配置驱动**：单例 ConfigManager + 全字段默认值，参数化阈值可灵活调优；

8. **标准化交叉编译**：CMake + toolchain + sysroot，一键构建到目标板。


---

## 15. 未来展望

- **模型升级**：可替换为更准的 YOLOv8n-pose 变体或引入多目标跟踪（ByteTrack）区分多人场景；

- **摔倒判定增强**：引入 LSTM/Transformer 时序模型，或用骨架图神经网络进一步提升区分度；

- **云端数据落地**：报警数据接入 MySQL/InfluxDB，实现历史统计与可视化大屏；

- **通知渠道打通**：对接微信小程序订阅消息 / 短信 / 电话告警；

- **OTA 与设备管理**：多网关接入设备管理平台，支持远程升级与配置下发；

- **多传感器融合**：结合毫米波雷达、红外等传感器，弥补摄像头在黑暗/遮挡场景的不足。