本章主要阐述“考研择校评估与考情查询小程序”的总体架构设计与技术方案。首先，设计系统的整体拓扑与逻辑架构，明确前端、后端与外部AI服务的交互机制；其次，对系统整体功能模块进行划分与演进设计；最后，深入开展数据库设计，包括概念结构设计（E-R图）和物理表结构的详细字段设计，为后续系统的详细设计与编码实现提供坚实的底层支撑。

## 3.1 系统架构设计

本系统采用前后端分离的开发模式，整体逻辑架构划分为展示层、业务逻辑层、数据持久层以及外部服务层。系统总体架构设计如图3-1所示。

> **【此处需插入论文插图：图3-1 系统总体逻辑架构图】**
> *（提示：图内应纵向划分为四层：① 展示层：UniApp微信小程序、Element Plus Web管理端；② 业务逻辑层：Spring Boot控制器与服务组件、RAG检索增强控制组件；③ 数据持久层：MyBatis-Plus、MySQL 8.0、Redis缓存；④ 外部服务层：大语言模型API接口）*

1. **展示层（Client Layer）**：面向普通考生的微信小程序端基于 UniApp + Vue3 构建，负责可视化图表（ECharts）的动态渲染、多校对比表单交互以及AI问答流式输出；面向管理员的后台管理端基于 Vue3 + Element Plus 构建，负责大规模考情数据的批量导入与清洗交互。
2. **业务逻辑层（Business Layer）**：采用 Spring Boot 框架实现核心业务。包含院校检索服务、多维归一化对比服务、分位数评测算服务。同时，集成 RAG（检索增强生成）工作流组件，负责在用户发起 AI 咨询时，拦截请求、提取实体、检索本地库并将数据包装为可信 Prompt。
3. **数据持久层（Data Layer）**：利用 MyBatis-Plus 简化单表数据访问，以 MySQL 8.0 作为关系型物理主库，存储核心招录指标与考生脱敏名单；引入 Redis 构筑高并发缓存屏障，承载报名季高频查询的学校字典与考情热点图表。
4. **外部服务层（External Service Layer）**：系统通过安全机制动态调用第三方大语言模型 API，在其接收到经过本地事实数据约束的上下文（Context）后，生成不具“幻觉”的择校规划与复习策略建议。

## 3.2 系统功能模块设计

根据需求分析结果，系统从视图视角划分为“微信小程序用户端”和“Web后台管理端”两大子系统。系统功能模块结构如图3-2所示。

> **【此处需插入论文插图：图3-2 系统功能模块结构图】**
> *（提示：采用树状组织结构图，顶层为系统名称，第二层拆分为小程序端和后台管理端，第三层展开各具体功能子模块）*

### 3.2.1 微信小程序用户端

1. **院校库查询模块**：提供基于省份、院校层次（985/211/双一流）、专业代码的多条件复合检索，并支持院校主页的深度招录指标（分数线、考试科目、招生简章）详情展示。
2. **多维对比分析模块**：支持将2-3所目标院校加入对比清单，由后端进行数据归一化处理后，以并列式表格和雷达图的形式直观呈现名额、分数线波动的横向差异。
3. **基于RAG的智能评测模块**：包含“上岸概率测算”和“AI智能问答”两个子功能。用户输入模拟成绩后系统自动匹配历史录取分位数，并联动大模型生成防幻觉的垂直招录答疑。

### 3.2.2 Web后台管理端

1. **考情数据治理模块**：集成 EasyExcel 组件，支持管理员批量导入、追加或覆盖更新《专业目录》、《一志愿统计》、《调剂来源》等庞大的招录 Excel/CSV 数据。
2. **系统维护模块**：提供用户备考数据的安全审计、核心字典项维护、高频热点缓存的主动刷新等管理功能。

## 3.3 数据库设计

### 3.3.1 数据库概念结构设计

根据考研择校系统的核心业务实体及其内在关联，设计了系统的全局概念模型（E-R图）。系统中核心实体包括：考研学生、院校专业目录、一志愿考情统计、调剂来源流向、拟录取名单、管理员。系统全局E-R图如图3-3所示。

> **【此处需插入论文插图：图3-3 系统全局实体关系（E-R）图】**

### 3.3.2 数据库物理表结构设计

本系统使用 MySQL 8.0 关系型数据库进行数据持久化存储。为了契合 MyBatis-Plus 框架的动态映射机制，现将各核心业务实体转换为规范化的物理表结构设计，具体完整结构如下：

#### 1. 用户基础信息表（`t_user`）

用于存储微信小程序端用户的核心画像、多端凭证（OpenID）以及预估分数信息。

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 描述 |
| --- | --- | --- | --- | --- | --- | --- |
| `id` | bigint | 20 | 是 | 否 | 无 | 唯一主键（雪花算法ID） |
| `openid` | varchar | 64 | 否 | 否 | 无 | 微信用户唯一标识 |
| `nickname` | varchar | 50 | 否 | 是 | '考研有志青年' | 用户微信昵称 |
| `avatar_url` | varchar | 255 | 否 | 是 | 无 | 用户头像链接 |
| `prelim_politics` | decimal | 5,2 | 否 | 是 | 0.00 | 预估/真实初试政治成绩 |
| `prelim_english` | decimal | 5,2 | 否 | 是 | 0.00 | 预估/真实初试英语成绩 |
| `prelim_math` | decimal | 5,2 | 否 | 是 | 0.00 | 预估/真实初试数学成绩 |
| `prelim_major` | decimal | 5,2 | 否 | 是 | 0.00 | 预估/真实初试专业课成绩 |
| `target_catalog_id` | bigint | 20 | 否 | 是 | 无 | 意向报考的专业目录外键ID |
| `create_time` | datetime | 0 | 否 | 否 | CURRENT_TIMESTAMP | 账号注册时间 |
| `update_time` | datetime | 0 | 否 | 否 | CURRENT_TIMESTAMP | 资料最后更新时间 |

#### 2. 院校专业目录表（`t_school_major_catalog`）

用于存储核心的院校招生大纲配置，是整个系统检索的基础。

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 描述 |
| --- | --- | --- | --- | --- | --- | --- |
| `id` | bigint | 20 | 是 | 否 | 无 | 目录主键ID |
| `school_code` | varchar | 10 | 否 | 否 | 无 | 院校代码 |
| `school_name` | varchar | 50 | 否 | 否 | 无 | 院校名称 |
| `province` | varchar | 20 | 否 | 否 | 无 | 院校所在省份 |
| `major_code` | varchar | 10 | 否 | 否 | 无 | 专业代码 |
| `major_name` | varchar | 50 | 否 | 否 | 无 | 专业名称 |
| `degree_type` | varchar | 10 | 否 | 否 | '专硕' | 学位类型（学硕/专硕） |
| `is_985` | tinyint | 1 | 否 | 否 | 0 | 是否为985高校(1-是,0-否) |
| `is_211` | tinyint | 1 | 否 | 否 | 0 | 是否为211高校(1-是,0-否) |
| `is_double_first` | tinyint | 1 | 否 | 否 | 0 | 是否双一流高校(1-是,0-否) |
| `study_type` | varchar | 10 | 否 | 否 | '全日制' | 学习形式（全日制/非全日制） |
| `exam_subjects` | varchar | 255 | 否 | 否 | 无 | 考试科目明细组合 |
| `research_direction` | text | 0 | 否 | 是 | 无 | 院系研究方向简介与备注说明 |
| `create_time` | datetime | 0 | 否 | 否 | CURRENT_TIMESTAMP | 数据导入创建时间 |

#### 3. 一志愿考情统计表（`t_first_choice_stats`）

记录特定专业历年一志愿的分数线波动和报录比趋势。

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 描述 |
| --- | --- | --- | --- | --- | --- | --- |
| `id` | bigint | 20 | 是 | 否 | 无 | 主键ID |
| `catalog_id` | bigint | 20 | 否 | 否 | 无 | 外键，关联专业目录表ID |
| `year` | int | 4 | 否 | 否 | 无 | 考情数据统计年份 |
| `apply_count` | int | 11 | 否 | 是 | 0 | 报考总人数 |
| `retest_count` | int | 11 | 否 | 是 | 0 | 进入复试的一志愿人数 |
| `admission_count` | int | 11 | 否 | 是 | 0 | 最终录取的初试一志愿人数 |
| `score_line` | int | 3 | 否 | 否 | 无 | 院校复试总分门槛线 |
| `max_score` | decimal | 5,2 | 否 | 是 | 0.00 | 录取一志愿初试最高分 |
| `min_score` | decimal | 5,2 | 否 | 是 | 0.00 | 录取一志愿初试最低分 |
| `avg_score` | decimal | 5,2 | 否 | 是 | 0.00 | 录取一志愿初试平均分 |
| `create_time` | datetime | 0 | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |

#### 4. 调剂考情统计表（`t_adjustment_stats`）

为考生提供历年专业接收调剂生源的门槛指标数据支撑。

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 描述 |
| --- | --- | --- | --- | --- | --- | --- |
| `id` | bigint | 20 | 是 | 否 | 无 | 主键ID |
| `catalog_id` | bigint | 20 | 否 | 否 | 无 | 外键，关联专业目录表ID |
| `year` | int | 4 | 否 | 否 | 无 | 调剂数据年份 |
| `quota_count` | int | 11 | 否 | 是 | 0 | 官方调剂名额总数 |
| `adj_max_score` | decimal | 5,2 | 否 | 是 | 0.00 | 调剂录取初试最高分 |
| `adj_min_score` | decimal | 5,2 | 否 | 是 | 0.00 | 调剂录取初试最低分 |
| `adj_avg_score` | decimal | 5,2 | 否 | 是 | 0.00 | 调剂录取初试加权平均分 |
| `create_time` | datetime | 0 | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |

#### 5. 调剂来源流向表（`t_adjustment_source`）

用于深度挖掘目标院校调剂生源的一志愿报考院校分布特征。

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 描述 |
| --- | --- | --- | --- | --- | --- | --- |
| `id` | bigint | 20 | 是 | 否 | 无 | 主键ID |
| `catalog_id` | bigint | 20 | 否 | 否 | 无 | 外键，关联专业目录表ID |
| `year` | int | 4 | 否 | 否 | 无 | 调剂流向统计年份 |
| `source_school` | varchar | 50 | 否 | 否 | 无 | 调剂生一志愿报考高校名称 |
| `source_level` | varchar | 20 | 否 | 是 | '其他' | 生源校层次(985/211/普本) |
| `student_count` | int | 11 | 否 | 否 | 1 | 该生源校成功调剂录取人数 |
| `create_time` | datetime | 0 | 否 | 否 | CURRENT_TIMESTAMP | 数据创建时间 |

#### 6. 考生拟录取明细表（`t_admission_detail`）

记录考生的初复试成绩明细，用于前端成绩散点图渲染与AI智能评测的分位数计算。

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 描述 |
| --- | --- | --- | --- | --- | --- | --- |
| `id` | bigint | 20 | 是 | 否 | 无 | 明细主键ID |
| `catalog_id` | bigint | 20 | 否 | 否 | 无 | 外键，关联专业目录表ID |
| `year` | int | 4 | 否 | 否 | 无 | 招录年份 |
| `candidate_no` | varchar | 20 | 否 | 否 | 无 | 脱敏考生编号 |
| `candidate_name` | varchar | 10 | 否 | 否 | 无 | 脱敏考生姓名 |
| `student_type` | varchar | 10 | 否 | 否 | '一志愿' | 考生类型(一志愿/调剂) |
| `preliminary_score` | decimal | 5,2 | 否 | 否 | 0.00 | 初试总成绩 |
| `retest_score` | decimal | 5,2 | 否 | 是 | 0.00 | 院校复试总成绩 |
| `total_score` | decimal | 5,2 | 否 | 否 | 0.00 | 初复试折算后最终总评成绩 |
| `ranking` | int | 5 | 否 | 是 | 无 | 综合成绩全系排名 |
| `admission_status` | tinyint | 1 | 否 | 否 | 0 | 状态(1-拟录取,0-淘汰) |
| `create_time` | datetime | 0 | 否 | 否 | CURRENT_TIMESTAMP | 创建时间 |

#### 7. 系统后台管理员表（`t_admin`）

用于Web管理端后台的账号登录与权限安全校验。

| 字段名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 默认值 | 描述 |
| --- | --- | --- | --- | --- | --- | --- |
| `id` | int | 11 | 是 | 否 | 无 | 管理员自增主键ID |
| `username` | varchar | 30 | 否 | 否 | 无 | 登录账号名称 |
| `password` | varchar | 100 | 否 | 否 | 无 | BCrypt加密保护密文 |
| `nickname` | varchar | 30 | 否 | 是 | 无 | 管理员真实姓名或昵称 |
| `status` | tinyint | 1 | 否 | 否 | 1 | 账户启用状态(1-正常,0-冻结) |
| `create_time` | datetime | 0 | 否 | 否 | CURRENT_TIMESTAMP | 账号生成时间 |

## 3.4 本章小结

本章完成了“考研择校评估与考情查询小程序”的总体架构设计。通过确立前后端分离的技术路线，科学划分了系统各功能模块，并通过实体关系模型（E-R图）和全量无省略的物理数据表字段设计，直观且规范地将爬取的数据集映射为系统底座，为下一章核心业务模块的高效编码实现提供了标准、坚实的技术规范依据。